(function () {
    angular.module('neows').controller('alertsController', [
        '$http',
        'moment',
        'asteroidService',
        AlertsController
    ]);

    function AlertsController($http, moment, asteroidService) {
        var vm = this

        //properties

        //functions

        //methods
        
        //init
    }
})()